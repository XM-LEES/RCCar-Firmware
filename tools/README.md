# Tools

Project-local automation inputs and debug skills.

## Skills

- `skills/ackermann-speed-pid-tuning/`: Ackermann speed PID tuning workflow,
  config, and test-record template.

Future tuning scripts must read the skill config or keep their defaults aligned
with it. Do not use legacy `vx/vy/vz` command defaults on the Ackermann branch.
