# 原始日志目录

本目录用于保存本地原始日志和临时调试抓包。

典型内容：

- 从 Windows 复制出的 Keil 编译日志。
- UART4 原始字节抓包。
- telemetry 解析输出。
- 静态检查终端转写。
- 示波器、逻辑分析仪或串口工具导出的临时文件。

除本 README 外，本目录文件默认被 `.gitignore` 忽略。确实需要提交某个原始日志时，先在 `../docs/test-records/` 写摘要，再用 `git add -f` 显式加入对应文件。
